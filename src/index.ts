/**
 * peer-pressure - Simple WebRTC peer connection library
 * Browser-only, zero-dependency WebRTC library
 */

export { default } from './peer'
export type {
  PeerOptions,
  SignalData,
  AddressInfo,
  PeerEvents,
  StatsReport,
  GetStatsCallback
} from './types'
